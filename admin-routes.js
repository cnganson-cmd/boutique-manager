function isAppAdmin(){return currentUser?.assignments?.some(a=>a.role==='ADMINISTRATEUR'&&!a.siteId)}
async function setStockRoute(typeId,sourceId,destinationId,active,button=null){if(!isAppAdmin())return;if(button){button.disabled=true;button.textContent='Enregistrement…'}try{await request('/rest/v1/rpc/configurer_route_flux_stock',{method:'POST',auth:true,body:{p_type_flux_stock_id:typeId,p_detenteur_source_id:sourceId,p_detenteur_destination_id:destinationId,p_actif:active}});await adminStockRoutes(active?'Route activée.':'Route désactivée.')}catch(err){shell(`<div class="title"><small>ERREUR</small><h1>Modification refusée</h1><p>${esc(err.message)}</p></div><button class="primary" onclick="adminStockRoutes()">Retour aux routes</button>`,'adminConsole()')}}
async function routeAdminData(){const [routes,holders,types,sites,users]=await Promise.all([authApi('routes_flux_stock','route_flux_stock_id,detenteur_source_id,detenteur_destination_id,type_flux_stock_id,actif'),authApi('detenteurs_stock','detenteur_stock_id,site_id,type_detenteur,destockeur_user_id,actif'),authApi('types_flux_stock','type_flux_stock_id,code_type_flux,nom_type_flux,actif'),authApi('sites','site_id,nom_site'),authApi('utilisateurs','user_id,nom')]),sm=Object.fromEntries(sites.map(s=>[s.site_id,s.nom_site])),um=Object.fromEntries(users.map(u=>[u.user_id,u.nom]));return {routes,holders,types,sites,hm:Object.fromEntries(holders.map(h=>[h.detenteur_stock_id,h.type_detenteur==='DESTOCKEUR'?`${um[h.destockeur_user_id]||'Déstockeur'} · mobile`:(sm[h.site_id]||'Site')]))}}
async function adminStockRoutes(message=''){if(!isAppAdmin())return;shell(`<div class="title"><small>ADMINISTRATION</small><h1>Routes de stock</h1><p>Chargement…</p></div>`,'adminConsole()');try{const d=await routeAdminData(),tm=Object.fromEntries(d.types.map(t=>[t.type_flux_stock_id,t.nom_type_flux||t.code_type_flux]));shell(`<div class="title"><small>ADMINISTRATION</small><h1>Routes de stock</h1><p>Définissez qui peut physiquement livrer qui, selon le type de flux.</p>${message?`<p><strong>${esc(message)}</strong></p>`:''}</div><button class="primary" onclick="adminNewRoute()">＋ Nouvelle route</button><section class="list">${d.routes.map(r=>`<article class="product-row"><div class="grow"><small>${esc(tm[r.type_flux_stock_id]||'Flux')}</small><strong>${esc(d.hm[r.detenteur_source_id]||'Source')} → ${esc(d.hm[r.detenteur_destination_id]||'Destination')}</strong><span>${r.actif?'✓ Active':'○ Inactive'}</span></div><div><button onclick="setStockRoute('${r.type_flux_stock_id}','${r.detenteur_source_id}','${r.detenteur_destination_id}',${!r.actif},this)">${r.actif?'Désactiver':'Activer'}</button><button onclick="adminDeleteRoute('${r.route_flux_stock_id}')">Supprimer</button></div></article>`).join('')||'<div class="empty">Aucune route configurée.</div>'}</section><div class="empty">Une route ayant déjà servi à un flux ne peut pas être supprimée : elle doit être désactivée afin de préserver l’historique.</div>`,'adminConsole()')}catch(err){shell(`<div class="title"><small>ERREUR</small><h1>Routes indisponibles</h1><p>${esc(err.message)}</p></div>`,'adminConsole()')}}
function routeDestinationOptions(){const source=document.getElementById('nr-source'),dest=document.getElementById('nr-dest');if(!source||!dest)return;[...dest.options].forEach(o=>{o.disabled=o.value===source.value});if(dest.value===source.value){const next=[...dest.options].find(o=>!o.disabled);dest.value=next?next.value:''}}
async function adminNewRoute(){try{const d=await routeAdminData(),activeH=d.holders.filter(h=>h.actif);shell(`<div class="title"><small>ADMINISTRATION</small><h1>Nouvelle route</h1><p>La destination doit être différente de la source. Le serveur vérifie aussi le sens métier du flux.</p></div><label>Type de flux<select id="nr-type">${d.types.filter(t=>t.actif).map(t=>`<option value="${t.type_flux_stock_id}">${esc(t.nom_type_flux||t.code_type_flux)}</option>`).join('')}</select></label><label>Source<select id="nr-source" onchange="routeDestinationOptions()">${activeH.map(h=>`<option value="${h.detenteur_stock_id}">${esc(d.hm[h.detenteur_stock_id])}</option>`).join('')}</select></label><label>Destination<select id="nr-dest">${activeH.map(h=>`<option value="${h.detenteur_stock_id}">${esc(d.hm[h.detenteur_stock_id])}</option>`).join('')}</select></label><button class="primary" onclick="adminSaveRoute()">Créer la route</button>`,'adminStockRoutes()');routeDestinationOptions()}catch(e){shell(`<div class="title"><h1>Création indisponible</h1><p>${esc(e.message)}</p></div>`,'adminStockRoutes()')}}
async function adminSaveRoute(){const source=document.getElementById('nr-source').value,dest=document.getElementById('nr-dest').value;if(!dest||source===dest){return shell(`<div class="title"><small>ROUTE REFUSÉE</small><h1>Destination incorrecte</h1><p>La source et la destination doivent être différentes.</p></div><button class="primary" onclick="adminNewRoute()">Corriger</button>`,'adminStockRoutes()')}try{await request('/rest/v1/rpc/configurer_route_flux_stock',{method:'POST',auth:true,body:{p_type_flux_stock_id:document.getElementById('nr-type').value,p_detenteur_source_id:source,p_detenteur_destination_id:dest,p_actif:true}});adminStockRoutes('Route créée et activée.')}catch(e){shell(`<div class="title"><small>ROUTE REFUSÉE</small><h1>Cette liaison n’est pas valide</h1><p>${esc(e.message)}</p></div><button class="primary" onclick="adminNewRoute()">Corriger</button>`,'adminStockRoutes()')}}
async function adminDeleteRoute(id){if(!confirm('Supprimer cette route ? Si elle a déjà servi, le serveur refusera pour préserver l’historique.'))return;try{await request('/rest/v1/rpc/admin_supprimer_route',{method:'POST',auth:true,body:{p_route_flux_stock_id:id}});adminStockRoutes('Route supprimée.')}catch(e){shell(`<div class="title"><small>ROUTE CONSERVÉE</small><h1>Suppression impossible</h1><p>${esc(e.message)}</p></div><button class="primary" onclick="adminStockRoutes()">Retour</button>`,'adminConsole()')}}
async function financialRouteAdminData(){const [routes,holders,sites,users]=await Promise.all([authApi('routes_financieres','route_financiere_id,detenteur_source_id,detenteur_destination_id,actif,cree_le','&order=cree_le.desc'),authApi('detenteurs_stock','detenteur_stock_id,type_detenteur,site_id,destockeur_user_id,actif'),authApi('sites','site_id,nom_site,actif'),authApi('utilisateurs','user_id,nom,actif')]),sm=Object.fromEntries(sites.map(s=>[s.site_id,s.nom_site])),um=Object.fromEntries(users.map(u=>[u.user_id,u.nom])),hm=Object.fromEntries(holders.map(h=>[h.detenteur_stock_id,h.type_detenteur==='DESTOCKEUR'?`${um[h.destockeur_user_id]||'Déstockeur'} · mobile`:(sm[h.site_id]||'Point de vente')]));return{routes,holders,hm}}
async function adminFinancialRoutes(message=''){if(!isAppAdmin())return;try{const d=await financialRouteAdminData();shell(`<div class="title"><small>ADMINISTRATION</small><h1>Routes financières</h1><p>Définissez à quel point de vente physique chaque source remet son argent.</p>${message?`<p><strong>${esc(message)}</strong></p>`:''}</div><button class="primary" onclick="adminNewFinancialRoute()">＋ Nouvelle route</button><section class="list">${d.routes.map(r=>`<article class="product-row admin-row"><div class="grow"><strong>${esc(d.hm[r.detenteur_source_id]||'Source')} → ${esc(d.hm[r.detenteur_destination_id]||'Destination')}</strong><span class="status-badge ${r.actif?'enabled':'disabled'}">${r.actif?'Active':'Inactive'}</span></div><button class="row-action" onclick="adminToggleFinancialRoute('${r.detenteur_source_id}','${r.detenteur_destination_id}',${!r.actif})">${r.actif?'Désactiver':'Activer'}</button></article>`).join('')||'<div class="empty">Aucune route financière.</div>'}</section><div class="empty">La destination est toujours un point physique. Le serveur bloque les routes vers un Déstockeur, les boucles et plusieurs destinations actives pour une même source.</div>`,'adminConsole()')}catch(e){adminError('Routes financières indisponibles',e)}}
async function adminNewFinancialRoute(){try{const d=await financialRouteAdminData(),sources=d.holders.filter(h=>h.actif),destinations=d.holders.filter(h=>h.actif&&h.type_detenteur==='SITE');shell(`<div class="title"><small>ADMINISTRATION</small><h1>Nouvelle route financière</h1><p>Une source mobile ou physique peut uniquement remettre à un point physique.</p></div><label>Source<select id="afr-source" onchange="financialDestinationOptions()">${sources.map(h=>`<option value="${h.detenteur_stock_id}">${esc(d.hm[h.detenteur_stock_id])}</option>`).join('')}</select></label><label>Destination physique<select id="afr-destination">${destinations.map(h=>`<option value="${h.detenteur_stock_id}">${esc(d.hm[h.detenteur_stock_id])}</option>`).join('')}</select></label><button class="primary" onclick="adminSaveFinancialRoute()">Activer cette route</button>`,'adminFinancialRoutes()');financialDestinationOptions()}catch(e){adminError('Création indisponible',e,'adminFinancialRoutes()')}}
function financialDestinationOptions(){const s=document.getElementById('afr-source'),d=document.getElementById('afr-destination');if(!s||!d)return;[...d.options].forEach(o=>o.disabled=o.value===s.value);if(d.selectedOptions[0]?.disabled)d.value=[...d.options].find(o=>!o.disabled)?.value||''}
async function adminSaveFinancialRoute(){const source=document.getElementById('afr-source').value,destination=document.getElementById('afr-destination').value;if(!destination||source===destination)return adminError('Route refusée','La destination physique doit être différente de la source.','adminNewFinancialRoute()');try{await request('/rest/v1/rpc/configurer_route_financiere',{method:'POST',auth:true,body:{p_detenteur_source_id:source,p_detenteur_destination_id:destination,p_actif:true}});adminFinancialRoutes('Route financière activée.')}catch(e){adminError('Route refusée',e,'adminNewFinancialRoute()')}}
async function adminToggleFinancialRoute(source,destination,active){try{await request('/rest/v1/rpc/configurer_route_financiere',{method:'POST',auth:true,body:{p_detenteur_source_id:source,p_detenteur_destination_id:destination,p_actif:active}});adminFinancialRoutes(active?'Route activée.':'Route désactivée.')}catch(e){adminError('Modification refusée',e,'adminFinancialRoutes()')}}
// Configuration des routes de stock et financières. Les contraintes de direction
// (notamment l'interdiction physique → mobile pour l'argent) sont imposées en base.
