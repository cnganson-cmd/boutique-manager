// Gestion du catalogue réservée à l'Administrateur global. La création complète
// reste atomique et contrôlée par la RPC afin d'éviter les produits incomplets.
async function adminCatalog(message=''){
  if(!adminOnly())return adminError('Accès non autorisé','La gestion du catalogue est réservée à l’Administrateur global.');
  try{
    const [items,brands,refs,manufacturers]=await Promise.all([
      authApi('produits','produit_id,nom_produit,variante,marque_id,categorie_produit_id,type_produit_id,fabricant_id,actif','&order=nom_produit.asc'),
      authApi('marques','marque_id,nom_marque'),
      authApi('references_produit','reference_produit_id,produit_id,sku_interne,libelle_reference,actif'),
      authApi('fabricants','fabricant_id,nom_fabricant,actif')
    ]),brandNames=Object.fromEntries(brands.map(x=>[x.marque_id,x.nom_marque])),manufacturerNames=Object.fromEntries(manufacturers.map(x=>[x.fabricant_id,x.nom_fabricant]));
    shell(`<div class="title"><small>ADMINISTRATION · CATALOGUE</small><h1>Produits</h1><p>${items.length} produit${items.length>1?'s':''} · ${refs.filter(x=>x.actif).length} référence${refs.filter(x=>x.actif).length>1?'s':''} active${refs.filter(x=>x.actif).length>1?'s':''}.</p>${message?`<p><strong>${esc(message)}</strong></p>`:''}</div><div class="admin-toolbar"><button class="primary" onclick="adminNewProduct()">＋ Nouveau produit</button><button onclick="adminLanaImports()">Imports à valider</button><button onclick="adminCatalogSettings()">Référentiels</button><button onclick="adminChooseProductImport()">Importer CSV</button><button onclick="adminExportProducts()">Exporter</button><input id="admin-product-import" type="file" accept=".csv,text/csv" hidden onchange="adminImportProducts(this.files[0])"></div><div class="admin-filters"><label for="admin-product-search">Rechercher<input id="admin-product-search" autocomplete="off" placeholder="Marque, produit, fabricant…" oninput="adminFilterProducts()"></label><label for="admin-product-status">Afficher<select id="admin-product-status" onchange="adminFilterProducts()"><option value="all">Tous</option><option value="active">Actifs</option><option value="inactive">Inactifs</option></select></label></div><section class="list" id="admin-product-list">${items.map(p=>{const productRefs=refs.filter(r=>r.produit_id===p.produit_id),manufacturer=manufacturerNames[p.fabricant_id]||'Fabricant non renseigné',search=`${brandNames[p.marque_id]||''} ${p.nom_produit} ${p.variante||''} ${manufacturer}`.toLowerCase();return `<article class="product-row admin-product-item" data-search="${esc(search)}" data-active="${Boolean(p.actif)}"><div class="grow"><small>${esc(brandNames[p.marque_id]||'Sans marque')} · ${p.actif?'ACTIF':'INACTIF'}</small><strong>${esc(p.nom_produit)}</strong><span>${esc(p.variante||'Sans variante')} · ${productRefs.length} référence${productRefs.length>1?'s':''}</span><span>Fabricant : ${esc(manufacturer)}</span></div><button class="row-action" onclick="adminEditProduct('${p.produit_id}')">Modifier</button></article>`}).join('')||'<div class="empty">Aucun produit.</div>'}</section>${nav('products')}`,'adminConsole()');
  }catch(e){adminError('Catalogue indisponible',e)}
}

async function adminNewProduct(candidateId=''){
  if(!adminOnly())return;
  try{
    const [brands,categories,types,manufacturers,suppliers,candidates]=await Promise.all([
      authApi('marques','marque_id,nom_marque,actif','&actif=eq.true&order=nom_marque.asc'),
      authApi('categories_produits','categorie_produit_id,nom_categorie,actif','&actif=eq.true&order=nom_categorie.asc'),
      authApi('types_produit','type_produit_id,nom_type,actif','&actif=eq.true&order=nom_type.asc'),
      authApi('fabricants','fabricant_id,nom_fabricant,actif','&actif=eq.true&order=nom_fabricant.asc'),
      authApi('fournisseurs','fournisseur_id,nom_fournisseur,actif','&actif=eq.true&order=nom_fournisseur.asc'),
      candidateId?authApi('candidats_import_catalogue','*',`&candidat_import_id=eq.${encodeURIComponent(candidateId)}`):Promise.resolve([])
    ]),candidate=candidates[0]||{},category=categories.find(x=>adminNormalize(x.nom_categorie)===adminNormalize(candidate.categorie_suggeree))?.categorie_produit_id||'',type=types.find(x=>adminNormalize(x.nom_type)===adminNormalize(candidate.type_suggere))?.type_produit_id||'',manufacturer=manufacturers.find(x=>adminNormalize(x.nom_fabricant)===adminNormalize(candidate.fabricant))?.fabricant_id||'',supplier=suppliers.find(x=>adminNormalize(x.nom_fournisseur)===adminNormalize(candidate.fournisseur))?.fournisseur_id||'';
    shell(`<div class="title"><small>${candidateId?'IMPORT À VALIDER':'NOUVEAU PRODUIT'}</small><h1>Ajouter au catalogue</h1><p>Vérifiez les informations avant de créer le produit et sa première référence.</p></div>${candidateId?`<div class="auto-value"><strong>Source Lana Bio Cosmetics</strong><span>Les informations proposées restent modifiables. <a href="${esc(candidate.source_url)}" target="_blank" rel="noopener">Voir la source</a></span></div>`:''}<datalist id="brand-options">${brands.map(x=>`<option value="${esc(x.nom_marque)}"></option>`).join('')}</datalist><label for="cp-brand">Marque</label><input id="cp-brand" list="brand-options" maxlength="100" autocomplete="off" placeholder="Choisir ou saisir une nouvelle marque" value="${esc(candidate.marque||'')}"><label for="cp-name">Nom du produit</label><input id="cp-name" maxlength="160" autocomplete="off" placeholder="Ex. Lait corps hydratant" value="${esc(candidate.nom_produit||'')}"><label for="cp-variant">Variante</label><input id="cp-variant" maxlength="160" autocomplete="off" placeholder="Ex. Karité · facultatif" value="${esc(candidate.variante||'')}"><label for="cp-manufacturer">Fabricant</label><select id="cp-manufacturer"><option value="">Non renseigné</option>${manufacturers.map(x=>`<option value="${x.fabricant_id}" ${x.fabricant_id===manufacturer?'selected':''}>${esc(x.nom_fabricant)}</option>`).join('')}</select><fieldset class="supplier-fieldset"><legend>Fournisseur(s)</legend>${suppliers.map(x=>`<label><input class="cp-supplier" type="checkbox" value="${x.fournisseur_id}" ${x.fournisseur_id===supplier?'checked':''}> ${esc(x.nom_fournisseur)}</label>`).join('')||'<p>Aucun fournisseur actif.</p>'}</fieldset><label for="cp-category">Catégorie</label><select id="cp-category"><option value="">Choisir une catégorie</option>${categories.map(x=>`<option value="${x.categorie_produit_id}" ${x.categorie_produit_id===category?'selected':''}>${esc(x.nom_categorie)}</option>`).join('')}</select><label for="cp-type">Type de produit</label><select id="cp-type"><option value="">Choisir un type</option>${types.map(x=>`<option value="${x.type_produit_id}" ${x.type_produit_id===type?'selected':''}>${esc(x.nom_type)}</option>`).join('')}</select><div class="form-section"><h2>Référence vendable</h2><div class="auto-value"><strong>SKU interne automatique</strong><span>Il sera généré à l’enregistrement, par exemple SAM-000001.</span></div><div class="form-pair"><div><label for="cp-size">Contenance</label><input id="cp-size" type="number" inputmode="decimal" min="0.01" step="0.01" placeholder="400" value="${esc(candidate.contenance_valeur||'')}"></div><div><label for="cp-unit">Unité</label><select id="cp-unit">${['ML','L','G','KG'].map(x=>`<option value="${x}" ${x===(candidate.unite_contenance||'ML')?'selected':''}>${x}</option>`).join('')}</select></div></div><label for="cp-barcode">Code-barres</label><input id="cp-barcode" inputmode="numeric" maxlength="40" autocomplete="off" placeholder="Facultatif"><label for="cp-lot">Unités vendues ensemble</label><input id="cp-lot" type="number" inputmode="numeric" min="1" step="1" value="1"><label for="cp-carton">Unités par carton</label><input id="cp-carton" type="number" inputmode="numeric" min="2" step="1" placeholder="Facultatif, ex. 24"></div><button class="primary" id="cp-save" onclick="adminSaveProduct('${candidateId}')">${candidateId?'Valider et créer':'Créer le produit'}</button>` ,candidateId?'adminLanaImports()':'adminCatalog()');
  }catch(e){adminError('Formulaire indisponible',e,'adminCatalog()')}
}

async function adminSaveProduct(candidateId=''){
  const value=id=>document.getElementById(id)?.value?.trim()||'',brand=value('cp-brand'),name=value('cp-name'),category=value('cp-category'),type=value('cp-type'),size=Number(value('cp-size')),lot=Number(value('cp-lot')),cartonText=value('cp-carton'),carton=cartonText?Number(cartonText):null,barcode=value('cp-barcode');
  if(!brand||!name)return alert('Renseignez la marque et le nom du produit.');
  if(!category||!type)return alert('Choisissez une catégorie et un type de produit.');
  if(!Number.isFinite(size)||size<=0)return alert('Indiquez une contenance supérieure à zéro.');
  if(!Number.isInteger(lot)||lot<1)return alert('Le nombre d’unités vendues ensemble doit être un entier positif.');
  if(carton!==null&&(!Number.isInteger(carton)||carton<2))return alert('Un carton doit contenir au moins 2 unités.');
  const button=document.getElementById('cp-save');button.disabled=true;button.textContent='Création…';
  try{
    const sku=await request('/rest/v1/rpc/admin_generer_sku_produit',{method:'POST',auth:true,body:{}});
    const supplierIds=[...document.querySelectorAll('.cp-supplier:checked')].map(x=>x.value),productId=await request('/rest/v1/rpc/admin_creer_produit_avec_approvisionnement',{method:'POST',auth:true,body:{p_nom_marque:brand,p_nom_produit:name,p_variante:value('cp-variant')||null,p_categorie_produit_id:category,p_type_produit_id:type,p_sku_interne:sku,p_code_externe:barcode||null,p_type_code_externe:barcode?'EAN13':null,p_contenance_valeur:size,p_unite_contenance:value('cp-unit'),p_quantite_dans_lot:lot,p_carton_multiplicateur:carton,p_fabricant_id:value('cp-manufacturer')||null,p_fournisseur_ids:supplierIds,p_candidat_import_id:candidateId||null}});
    products=[];catalogReady=false;packagingByRef={};void loadCatalog();
    shell(`<div class="success"><div>✓</div><h1>Produit créé</h1><p>${esc(brand)} · ${esc(name)} est maintenant disponible dans le catalogue.</p><p><strong>SKU : ${esc(sku)}</strong></p><button class="primary" onclick="adminCatalog()">Retour au catalogue</button></div>`);
  }catch(e){button.disabled=false;button.textContent='Créer le produit';alert(e.message)}
}
