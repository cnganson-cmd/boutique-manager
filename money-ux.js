function moneyFmt(n){return new Intl.NumberFormat('fr-FR').format(Number(n||0))+' FCFA'}
function moneyOp(key){const k='bm_money_'+key;let v=sessionStorage.getItem(k);if(!v){v=crypto.randomUUID();sessionStorage.setItem(k,v)}return v}function moneyDone(key){sessionStorage.removeItem('bm_money_'+key)}
function moneySite(){return currentSiteContext()?.id||''}
function moneyFields(){return '<label>Cash (FCFA)</label><input id="m-cash" type="number" min="0" value="0"><label>Mobile Money (FCFA)</label><input id="m-momo" type="number" min="0" value="0"><label>Commentaire</label><input id="m-comment" placeholder="Facultatif">'}
async function moneyHome(){const ctx=currentSiteContext();if(!ctx)return home();shell('<div class="title"><small>ARGENT · '+esc(ctx.name)+'</small><h1>Recettes & remises</h1><p>Cash et Mobile Money sont suivis séparément.</p></div><section class="grid">'+actionCard('＋','Déclarer ma recette','Recette du jour','moneyRevenue()',true)+(hasRoleHere('GERANT')?actionCard('📥','Remise reçue','Enregistrer une remise reçue','moneyReceive()')+actionCard('−','Demander un retrait','Soumis au Patron','moneyWithdrawal()'):'')+'</section><button class="primary" onclick="moneyTasks()">Opérations à traiter</button><button class="primary secondary" onclick="moneyHistory()">Voir mon historique</button>','home()')}
function moneyRevenue(){shell('<div class="title"><small>RECETTE</small><h1>Déclarer la recette</h1><p>'+esc(currentSiteContext()?.name)+'</p></div>'+moneyFields()+'<button class="primary" onclick="submitMoneyRevenue()">Enregistrer</button>','moneyHome()')}
async function submitMoneyRevenue(){const key='revenue-'+moneySite()+'-'+new Date().toISOString().slice(0,10);try{await request('/rest/v1/rpc/declarer_recette',{method:'POST',auth:true,body:{p_operation_client_id:moneyOp(key),p_site_id:moneySite(),p_date:new Date().toISOString().slice(0,10),p_cash:+document.querySelector('#m-cash').value,p_mobile_money:+document.querySelector('#m-momo').value,p_commentaire:document.querySelector('#m-comment').value||null}});moneyDone(key);shell('<div class="success"><div>✓</div><h1>Recette enregistrée</h1><button class="primary" onclick="moneyHome()">Continuer</button></div>')}catch(e){alert(e.message)}}
async function moneyReceive(){try{const routes=await authApi('routes_financieres','route_financiere_id,site_source_id,site_destination_id','&site_destination_id=eq.'+moneySite()+'&actif=eq.true');if(!routes.length)throw new Error('Aucune remise autorisée vers ce site');const sites=await authApi('sites','site_id,nom_site');const sm=Object.fromEntries(sites.map(s=>[s.site_id,s.nom_site]));shell('<div class="title"><small>REMISE REÇUE</small><h1>Qui vous remet l’argent ?</h1></div><label>Point de vente</label><select id="m-source">'+routes.map(r=>'<option value="'+r.site_source_id+'">'+esc(sm[r.site_source_id]||'Site')+'</option>').join('')+'</select>'+moneyFields()+'<button class="primary" onclick="submitMoneyReceive()">Enregistrer ce que j’ai reçu</button>','moneyHome()')}catch(e){shell('<div class="title"><h1>Remise indisponible</h1><p>'+esc(e.message)+'</p></div>','moneyHome()')}}
async function submitMoneyReceive(){const src=document.querySelector('#m-source').value,key='remise-'+src+'-'+moneySite();try{await request('/rest/v1/rpc/creer_remise_argent',{method:'POST',auth:true,body:{p_operation_client_id:moneyOp(key),p_site_source_id:src,p_site_destination_id:moneySite(),p_cash:+document.querySelector('#m-cash').value,p_mobile_money:+document.querySelector('#m-momo').value,p_commentaire:document.querySelector('#m-comment').value||null}});moneyDone(key);shell('<div class="success"><div>✓</div><h1>Remise à confirmer</h1><p>Le gérant du point de vente source doit confirmer le montant.</p><button class="primary" onclick="moneyHome()">Continuer</button></div>')}catch(e){alert(e.message)}}
function moneyWithdrawal(){shell('<div class="title"><small>RETRAIT</small><h1>Demander un retrait</h1><p>Le Patron devra valider cette sortie.</p></div>'+moneyFields()+'<label>Motif</label><input id="m-reason" placeholder="Ex. paiement fournisseur"><button class="primary" onclick="submitMoneyWithdrawal()">Envoyer au Patron</button>','moneyHome()')}
async function submitMoneyWithdrawal(){const key='withdraw-'+moneySite();try{await request('/rest/v1/rpc/creer_retrait_argent',{method:'POST',auth:true,body:{p_operation_client_id:moneyOp(key),p_site_id:moneySite(),p_cash:+document.querySelector('#m-cash').value,p_mobile_money:+document.querySelector('#m-momo').value,p_motif:document.querySelector('#m-reason').value}});moneyDone(key);shell('<div class="success"><div>✓</div><h1>Retrait envoyé</h1><p>En attente de validation du Patron.</p><button class="primary" onclick="moneyHome()">Continuer</button></div>')}catch(e){alert(e.message)}}
async function moneyHistory(){try{const [r,f]=await Promise.all([authApi('recettes','recette_id,date_recette,montant_cash,montant_mobile_money,cree_le','&site_id=eq.'+moneySite()+'&order=cree_le.desc&limit=20'),authApi('flux_argent','flux_argent_id,type_flux,montant_cash,montant_mobile_money,statut,cree_le','&order=cree_le.desc&limit=30')]);shell('<div class="title"><small>ARGENT</small><h1>Historique</h1></div><section class="list">'+r.map(x=>'<article class="product-row"><div class="grow"><small>RECETTE · '+esc(x.date_recette)+'</small><strong>'+moneyFmt(+x.montant_cash + +x.montant_mobile_money)+'</strong><span>Cash '+moneyFmt(x.montant_cash)+' · Mobile Money '+moneyFmt(x.montant_mobile_money)+'</span></div></article>').join('')+f.map(x=>'<article class="product-row"><div class="grow"><small>'+esc(x.type_flux)+' · '+esc(moneyStatus(x.statut))+'</small><strong>'+moneyFmt(+x.montant_cash + +x.montant_mobile_money)+'</strong><span>Cash '+moneyFmt(x.montant_cash)+' · Mobile Money '+moneyFmt(x.montant_mobile_money)+'</span></div><button onclick="patronMoneyAction(\''+x.flux_argent_id+'\')">→</button></article>').join('')+'</section>','moneyHome()')}catch(e){shell('<div class="title"><h1>Historique indisponible</h1><p>'+esc(e.message)+'</p></div>','moneyHome()')}}
async function patronMoney(){try{const f=await authApi('flux_argent','flux_argent_id,type_flux,montant_cash,montant_mobile_money,statut,motif,cree_le','&order=cree_le.desc&limit=50');shell('<div class="title"><small>PATRON · ARGENT</small><h1>Vue financière</h1><p>Retraits, remises et anomalies.</p></div><section class="list">'+f.map(x=>{const actionable=(x.type_flux==='RETRAIT'&&x.statut==='A_VALIDER_PATRON')||(x.type_flux==='REMISE'&&x.statut==='ANOMALIE_PATRON');return '<article class="product-row"><div class="grow"><small>'+esc(x.type_flux)+' · '+esc(moneyStatus(x.statut))+'</small><strong>'+moneyFmt(+x.montant_cash + +x.montant_mobile_money)+'</strong><span>Cash '+moneyFmt(x.montant_cash)+' · Mobile Money '+moneyFmt(x.montant_mobile_money)+'</span></div>'+(actionable?'<button aria-label="Traiter cette opération" onclick="patronMoneyAction(\''+x.flux_argent_id+'\')">→</button>':'')+'</article>'}).join('')+(f.length?'':'<div class="empty">Aucune opération financière.</div>')+'</section>','home()')}catch(e){shell('<div class="title"><h1>Vue indisponible</h1><p>'+esc(e.message)+'</p></div>','home()')}}
function moneyStatus(s){return ({A_CONFIRMER_SOURCE:'À confirmer',ANOMALIE_CORRIGEABLE:'Contesté · correction possible',ANOMALIE_PATRON:'À arbitrer par le Patron',A_VALIDER_PATRON:'À valider par le Patron',CLOTURE:'Clôturé',REFUSE:'Refusé'})[s]||s}
async function moneyTasks(){const site=moneySite();try{const f=await authApi('flux_argent','flux_argent_id,type_flux,site_source_id,site_destination_id,montant_cash,montant_mobile_money,statut,motif,cree_le','&order=cree_le.desc&limit=50');const tasks=f.filter(x=>(x.type_flux==='REMISE'&&x.site_source_id===site&&x.statut==='A_CONFIRMER_SOURCE')||(x.type_flux==='REMISE'&&x.site_destination_id===site&&x.statut==='ANOMALIE_CORRIGEABLE'));shell('<div class="title"><small>ARGENT</small><h1>À traiter</h1><p>'+tasks.length+' opération'+(tasks.length>1?'s':'')+' nécessite'+(tasks.length>1?'nt':'')+' votre attention.</p></div><section class="list">'+tasks.map(x=>'<article class="product-row"><div class="grow"><small>'+esc(moneyStatus(x.statut))+'</small><strong>'+moneyFmt(+x.montant_cash + +x.montant_mobile_money)+'</strong><span>Cash '+moneyFmt(x.montant_cash)+' · Mobile Money '+moneyFmt(x.montant_mobile_money)+'</span></div><button onclick="moneyTaskDetail(\''+x.flux_argent_id+'\')">→</button></article>').join('')+(tasks.length?'':'<div class="empty">Rien à traiter.</div>')+'</section>','moneyHome()')}catch(e){shell('<div class="title"><h1>Indisponible</h1><p>'+esc(e.message)+'</p></div>','moneyHome()')}}
async function moneyTaskDetail(id){const rows=await authApi('flux_argent','flux_argent_id,type_flux,site_source_id,site_destination_id,montant_cash,montant_mobile_money,statut','&flux_argent_id=eq.'+id);if(!rows.length)return moneyTasks();const x=rows[0],source=x.site_source_id===moneySite();let actions='';if(source&&x.statut==='A_CONFIRMER_SOURCE')actions='<button class="primary" onclick="answerMoneyRemise(\''+id+'\',\'CONFIRME\')">Je confirme ce montant</button><button class="primary secondary" onclick="contestMoneyRemise(\''+id+'\')">Je conteste</button>';if(!source&&x.statut==='ANOMALIE_CORRIGEABLE')actions='<button class="primary" onclick="correctMoneyRemise(\''+id+'\')">Corriger mon montant</button><button class="primary secondary" onclick="escalateMoneyRemise(\''+id+'\')">Je maintiens · envoyer au Patron</button>';shell('<div class="title"><small>REMISE · '+esc(moneyStatus(x.statut))+'</small><h1>'+moneyFmt(+x.montant_cash + +x.montant_mobile_money)+'</h1><p>Cash '+moneyFmt(x.montant_cash)+' · Mobile Money '+moneyFmt(x.montant_mobile_money)+'</p></div>'+actions,'moneyTasks()')}
async function answerMoneyRemise(id,decision,cash=null,momo=null,comment=null){const key='answer-'+id+'-'+decision;try{await request('/rest/v1/rpc/repondre_remise_argent',{method:'POST',auth:true,body:{p_operation_client_id:moneyOp(key),p_flux_argent_id:id,p_decision:decision,p_cash:cash,p_mobile_money:momo,p_commentaire:comment}});moneyDone(key);moneyTasks()}catch(e){alert(e.message)}}
function contestMoneyRemise(id){shell('<div class="title"><small>CONTESTER</small><h1>Quel montant avez-vous remis ?</h1></div>'+moneyFields()+'<button class="primary" onclick="submitContestMoney(\''+id+'\')">Contester</button>','moneyTaskDetail(\''+id+'\')')}
function submitContestMoney(id){answerMoneyRemise(id,'CONTESTE',+document.querySelector('#m-cash').value,+document.querySelector('#m-momo').value,document.querySelector('#m-comment').value||null)}
function correctMoneyRemise(id){shell('<div class="title"><small>CORRECTION</small><h1>Corriger le montant reçu</h1><p>Le Kiosque devra confirmer à nouveau.</p></div>'+moneyFields()+'<button class="primary" onclick="submitCorrectMoney(\''+id+'\')">Enregistrer la correction</button>','moneyTaskDetail(\''+id+'\')')}
async function submitCorrectMoney(id){const key='correct-'+id;try{await request('/rest/v1/rpc/corriger_remise_argent',{method:'POST',auth:true,body:{p_operation_client_id:moneyOp(key),p_flux_argent_id:id,p_cash:+document.querySelector('#m-cash').value,p_mobile_money:+document.querySelector('#m-momo').value,p_commentaire:document.querySelector('#m-comment').value||null}});moneyDone(key);moneyTasks()}catch(e){alert(e.message)}}
async function escalateMoneyRemise(id){const key='escalate-'+id;try{await request('/rest/v1/rpc/escalader_remise_argent',{method:'POST',auth:true,body:{p_operation_client_id:moneyOp(key),p_flux_argent_id:id,p_commentaire:'Montant maintenu par le gérant destinataire'}});moneyDone(key);moneyTasks()}catch(e){alert(e.message)}}
async function patronMoneyAction(id){const rows=await authApi('flux_argent','flux_argent_id,type_flux,montant_cash,montant_mobile_money,statut,motif','&flux_argent_id=eq.'+id);if(!rows.length)return patronMoney();const x=rows[0];let a='';if(x.type_flux==='RETRAIT'&&x.statut==='A_VALIDER_PATRON')a='<button class="primary" onclick="decideWithdrawal(\''+id+'\',\'VALIDE\')">Valider le retrait</button><button class="primary secondary" onclick="decideWithdrawal(\''+id+'\',\'REFUSE\')">Refuser</button>';if(x.type_flux==='REMISE'&&x.statut==='ANOMALIE_PATRON')a=moneyFields()+'<button class="primary" onclick="resolveMoneyAnomaly(\''+id+'\')">Clôturer avec ce montant</button>';shell('<div class="title"><small>'+esc(x.type_flux)+' · '+esc(moneyStatus(x.statut))+'</small><h1>'+moneyFmt(+x.montant_cash + +x.montant_mobile_money)+'</h1><p>'+esc(x.motif||'')+'</p></div>'+a,'patronMoney()')}
async function decideWithdrawal(id,d){const key='patron-withdraw-'+id+'-'+d;try{await request('/rest/v1/rpc/valider_retrait_argent',{method:'POST',auth:true,body:{p_operation_client_id:moneyOp(key),p_flux_argent_id:id,p_decision:d,p_commentaire:null}});moneyDone(key);patronMoney()}catch(e){alert(e.message)}}
async function resolveMoneyAnomaly(id){const key='patron-anomaly-'+id;try{await request('/rest/v1/rpc/resoudre_anomalie_argent_patron',{method:'POST',auth:true,body:{p_operation_client_id:moneyOp(key),p_flux_argent_id:id,p_cash:+document.querySelector('#m-cash').value,p_mobile_money:+document.querySelector('#m-momo').value,p_commentaire:document.querySelector('#m-comment').value||'Arbitrage Patron'}});moneyDone(key);patronMoney()}catch(e){alert(e.message)}}
async function moneyCurrentHolder(){const ctx=currentSiteContext();if(ctx){const rows=await authApi('detenteurs_stock','detenteur_stock_id,type_detenteur,site_id,destockeur_user_id,actif',`&site_id=eq.${encodeURIComponent(ctx.id)}&actif=eq.true`);return rows[0]||null}const rows=await authApi('detenteurs_stock','detenteur_stock_id,type_detenteur,site_id,destockeur_user_id,actif',`&destockeur_user_id=eq.${encodeURIComponent(currentUser.user_id)}&actif=eq.true`);return rows[0]||null}
async function moneyHolderLabels(holders){const siteIds=[...new Set(holders.map(h=>h.site_id).filter(Boolean))],userIds=[...new Set(holders.map(h=>h.destockeur_user_id).filter(Boolean))],[sites,users]=await Promise.all([siteIds.length?authApi('sites','site_id,nom_site',`&site_id=in.(${siteIds.join(',')})`):[],userIds.length?authApi('utilisateurs','user_id,nom',`&user_id=in.(${userIds.join(',')})`):[]]),sm=Object.fromEntries(sites.map(s=>[s.site_id,s.nom_site])),um=Object.fromEntries(users.map(u=>[u.user_id,u.nom]));return Object.fromEntries(holders.map(h=>[h.detenteur_stock_id,h.type_detenteur==='DESTOCKEUR'?`${um[h.destockeur_user_id]||'Déstockeur'} · mobile`:(sm[h.site_id]||'Point de vente')]))}
async function destockerMoneyHome(){const holder=await moneyCurrentHolder();if(!holder)return shell('<div class="title"><h1>Caisse mobile indisponible</h1><p>Aucun détenteur mobile actif ne correspond à ce compte.</p></div>','home()');shell(`<div class="title"><small>ARGENT · DÉSTOCKEUR MOBILE</small><h1>Ma caisse</h1><p>Confirmez les remises enregistrées par votre point de collecte.</p></div><section class="grid">${actionCard('✓','Remises à confirmer','Confirmer ou contester les montants','moneyTasks()',true)}${actionCard('🕘','Historique','Consulter mes remises','moneyHistory()')}</section>`,'home()')}
async function moneyReceive(){try{const destination=await moneyCurrentHolder();if(!destination||destination.type_detenteur!=='SITE')throw new Error('Le destinataire doit être un point de vente physique');const routes=await authApi('routes_financieres','route_financiere_id,detenteur_source_id,detenteur_destination_id','&detenteur_destination_id=eq.'+destination.detenteur_stock_id+'&actif=eq.true');if(!routes.length)throw new Error('Aucune remise autorisée vers ce point de vente');const ids=[...new Set(routes.flatMap(r=>[r.detenteur_source_id,r.detenteur_destination_id]))],holders=await authApi('detenteurs_stock','detenteur_stock_id,type_detenteur,site_id,destockeur_user_id,actif',`&detenteur_stock_id=in.(${ids.join(',')})`),labels=await moneyHolderLabels(holders);window._moneyDestinationHolder=destination.detenteur_stock_id;shell('<div class="title"><small>REMISE REÇUE</small><h1>Qui vous remet l’argent ?</h1></div><label>Source financière</label><select id="m-source">'+routes.map(r=>'<option value="'+r.detenteur_source_id+'">'+esc(labels[r.detenteur_source_id]||'Source')+'</option>').join('')+'</select>'+moneyFields()+'<button class="primary" onclick="submitMoneyReceive()">Enregistrer ce que j’ai reçu</button>','moneyHome()')}catch(e){shell('<div class="title"><h1>Remise indisponible</h1><p>'+esc(e.message)+'</p></div>','moneyHome()')}}
async function submitMoneyReceive(){const src=document.querySelector('#m-source').value,dst=window._moneyDestinationHolder,key='remise-holder-'+src+'-'+dst;try{await request('/rest/v1/rpc/creer_remise_argent_detenteurs',{method:'POST',auth:true,body:{p_operation_client_id:moneyOp(key),p_detenteur_source_id:src,p_detenteur_destination_id:dst,p_cash:+document.querySelector('#m-cash').value,p_mobile_money:+document.querySelector('#m-momo').value,p_commentaire:document.querySelector('#m-comment').value||null}});moneyDone(key);shell('<div class="success"><div>✓</div><h1>Remise à confirmer</h1><p>Le responsable de la source doit confirmer le montant.</p><button class="primary" onclick="moneyHome()">Continuer</button></div>')}catch(e){alert(e.message)}}
async function moneyTasks(){try{const holder=await moneyCurrentHolder();if(!holder)throw new Error('Détenteur financier introuvable');const f=await authApi('flux_argent','flux_argent_id,type_flux,detenteur_source_id,detenteur_destination_id,montant_cash,montant_mobile_money,statut,motif,cree_le','&order=cree_le.desc&limit=50'),tasks=f.filter(x=>(x.type_flux==='REMISE'&&x.detenteur_source_id===holder.detenteur_stock_id&&x.statut==='A_CONFIRMER_SOURCE')||(x.type_flux==='REMISE'&&x.detenteur_destination_id===holder.detenteur_stock_id&&x.statut==='ANOMALIE_CORRIGEABLE'));shell('<div class="title"><small>ARGENT</small><h1>À traiter</h1><p>'+tasks.length+' opération'+(tasks.length>1?'s':'')+' nécessite'+(tasks.length>1?'nt':'')+' votre attention.</p></div><section class="list">'+tasks.map(x=>'<article class="product-row"><div class="grow"><small>'+esc(moneyStatus(x.statut))+'</small><strong>'+moneyFmt(+x.montant_cash + +x.montant_mobile_money)+'</strong><span>Cash '+moneyFmt(x.montant_cash)+' · Mobile Money '+moneyFmt(x.montant_mobile_money)+'</span></div><button onclick="moneyTaskDetail(\''+x.flux_argent_id+'\')">→</button></article>').join('')+(tasks.length?'':'<div class="empty">Rien à traiter.</div>')+'</section>',hasRoleHere('DESTOCKEUR')?'destockerMoneyHome()':'moneyHome()')}catch(e){shell('<div class="title"><h1>Indisponible</h1><p>'+esc(e.message)+'</p></div>','home()')}}
async function moneyTaskDetail(id){const holder=await moneyCurrentHolder(),rows=await authApi('flux_argent','flux_argent_id,type_flux,detenteur_source_id,detenteur_destination_id,montant_cash,montant_mobile_money,statut','&flux_argent_id=eq.'+id);if(!rows.length)return moneyTasks();const x=rows[0],source=x.detenteur_source_id===holder?.detenteur_stock_id;let actions='';if(source&&x.statut==='A_CONFIRMER_SOURCE')actions='<button class="primary" onclick="answerMoneyRemise(\''+id+'\',\'CONFIRME\')">Je confirme ce montant</button><button class="primary secondary" onclick="contestMoneyRemise(\''+id+'\')">Je conteste</button>';if(!source&&x.statut==='ANOMALIE_CORRIGEABLE')actions='<button class="primary" onclick="correctMoneyRemise(\''+id+'\')">Corriger mon montant</button><button class="primary secondary" onclick="escalateMoneyRemise(\''+id+'\')">Je maintiens · envoyer au Patron</button>';shell('<div class="title"><small>REMISE · '+esc(moneyStatus(x.statut))+'</small><h1>'+moneyFmt(+x.montant_cash + +x.montant_mobile_money)+'</h1><p>Cash '+moneyFmt(x.montant_cash)+' · Mobile Money '+moneyFmt(x.montant_mobile_money)+'</p></div>'+actions,'moneyTasks()')}
async function moneyHistory(){try{const holder=await moneyCurrentHolder();if(!holder)throw new Error('Détenteur financier introuvable');const f=await authApi('flux_argent','flux_argent_id,type_flux,detenteur_source_id,detenteur_destination_id,montant_cash,montant_mobile_money,statut,cree_le','&order=cree_le.desc&limit=30');shell('<div class="title"><small>ARGENT</small><h1>Historique</h1></div><section class="list">'+f.map(x=>'<article class="product-row"><div class="grow"><small>'+esc(x.type_flux)+' · '+esc(moneyStatus(x.statut))+'</small><strong>'+moneyFmt(+x.montant_cash + +x.montant_mobile_money)+'</strong><span>Cash '+moneyFmt(x.montant_cash)+' · Mobile Money '+moneyFmt(x.montant_mobile_money)+'</span></div></article>').join('')+(f.length?'':'<div class="empty">Aucune opération.</div>')+'</section>',hasRoleHere('DESTOCKEUR')?'destockerMoneyHome()':'moneyHome()')}catch(e){shell('<div class="title"><h1>Historique indisponible</h1><p>'+esc(e.message)+'</p></div>','home()')}}
